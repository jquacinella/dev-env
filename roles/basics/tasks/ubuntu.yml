---
- name: Install basic dependencies
  tags: ['basics', 'git', 'curl', 'wget', 'build-essential', 'zsh', 'tmux', 'ccze', 'direnv', 'redshift', 'xsel', 'fonts']
  apt:
    name:
      - git
      - curl
      - wget
      - build-essential
      - zsh
      - tmux
      - ccze
      - direnv
      - redshift
      - xsel
      - fonts-ibm-plex
    state: present
